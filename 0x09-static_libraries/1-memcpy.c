#include "main.h"

/**
 * _memcpy - copies the memory area from source to destination.
 * @dest: pointer to the destination array
 * @src: pointer to source array.
 * @n: number of bytes to be copied.
 *
 * Return: Always 0.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (0);
}
