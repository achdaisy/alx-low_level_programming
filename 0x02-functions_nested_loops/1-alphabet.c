#include "main.h"

/**
 * print_alphabet - prints lowercase alphabet from a to z
 * Return: Always 0.
 */
void print_alphabet(void)
{
char letter;
for (letter = 'a'; letter <= letter; letter++)
{
	_putchar(letter);
}
_putchar('\n');
return (0);
}
